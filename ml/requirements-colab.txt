# Installed by the first Colab notebook cell. Colab's default runtime
# already ships a matching torch/CUDA build — installing ultralytics with
# its normal dependency resolution can pull a different torch and silently
# break GPU support, so the notebook installs these with care (see
# ml/notebooks/AgriSense_PlantDisease_Colab.ipynb cell 1) rather than a
# plain `pip install -r`.
ultralytics>=8.3.0
onnx>=1.16.0
onnxruntime-gpu>=1.18.0
imagehash>=4.3.1
pillow>=10.3.0
pandas>=2.2.0
scikit-learn>=1.5.0
pyyaml>=6.0
tqdm>=4.66.0
kaggle>=1.6.14
matplotlib>=3.8.0
pytest>=8.0.0
